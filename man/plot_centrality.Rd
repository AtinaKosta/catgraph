% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/centrality.R
\name{plot_centrality}
\alias{plot_centrality}
\title{Plot weighted centrality indices for a catgraph}
\usage{
plot_centrality(
  x,
  measure = "strength",
  normalize = TRUE,
  title = NULL,
  color = "#7F77DD",
  engine = c("ggplot2", "base")
)
}
\arguments{
\item{x}{A \code{catgraph} object.}

\item{measure}{Character. One of \code{"strength"}, \code{"w_betweenness"},
\code{"w_closeness"}, \code{"w_eigenvector"}, \code{"w_pagerank"}, or
\code{"all"}. Default \code{"strength"}.}

\item{normalize}{Logical. Passed to \code{\link{node_centrality}}.
Default \code{TRUE}.}

\item{title}{Character. Plot title. Default \code{NULL}.}

\item{color}{Character. Bar fill colour. Default \code{"#7F77DD"}
(purple-400).}

\item{engine}{Character. \code{"ggplot2"} (default) or \code{"base"}.}
}
\value{
For \code{engine = "ggplot2"}: a \code{ggplot} object.
  For \code{engine = "base"}: \code{NULL}, invisibly.
}
\description{
Produces a ranked horizontal bar chart of one or all centrality measures
from a \code{catgraph} object. When a single measure is selected the chart
shows bars ranked by that measure. When \code{measure = "all"}, a faceted
or overlaid comparison chart is produced.
}
\examples{
df <- expand_table(Titanic)
cg <- catgraph(df)
plot_centrality(cg)
plot_centrality(cg, measure = "w_betweenness")
# plot_centrality(cg, measure = "all")  # requires ggplot2

}
\seealso{
\code{\link{node_centrality}}
}
