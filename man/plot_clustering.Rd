% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clustering_coef.R
\name{plot_clustering}
\alias{plot_clustering}
\title{Plot weighted clustering coefficients for a catgraph}
\usage{
plot_clustering(
  x,
  method = "barrat",
  normalize = TRUE,
  title = NULL,
  engine = c("ggplot2", "base")
)
}
\arguments{
\item{x}{A \code{catgraph} object.}

\item{method}{Character. One method name or \code{"all"}. Default
\code{"barrat"}.}

\item{normalize}{Logical. Default \code{TRUE}.}

\item{title}{Character. Plot title. Default \code{NULL}.}

\item{engine}{Character. \code{"ggplot2"} (default) or \code{"base"}.}
}
\value{
For \code{engine = "ggplot2"}: a \code{ggplot} object.
  For \code{engine = "base"}: \code{NULL}, invisibly.
}
\description{
Produces a bar chart (single method) or a grouped/faceted comparison
chart (multiple methods) of clustering coefficients.
}
\examples{
df <- expand_table(Titanic)
cg <- catgraph(df)
plot_clustering(cg)
plot_clustering(cg, method = "all")

}
\seealso{
\code{\link{clustering_coef}}, \code{\link{compare_clustering}}
}
