% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/catgraph.R
\name{plot.catgraph}
\alias{plot.catgraph}
\title{Plot a \code{catgraph} object}
\usage{
\method{plot}{catgraph}(
  x,
  engine = c("igraph", "ggraph"),
  layout = "fr",
  edge_width_range = c(0.5, 4),
  vertex_size = 30,
  vertex_color = "#AFA9EC",
  edge_color = "#888780",
  label_size = 0.8,
  title = NULL,
  ...
)
}
\arguments{
\item{x}{A \code{catgraph} object.}

\item{engine}{Character. Either \code{"igraph"} (default) or \code{"ggraph"}.}

\item{layout}{Character. Layout algorithm. For \code{engine = "igraph"},
one of \code{"fr"} (Fruchterman-Reingold, default), \code{"kk"}
(Kamada-Kawai), \code{"circle"}, \code{"grid"}, \code{"graphopt"},
\code{"nicely"}, or \code{"random"}. For \code{engine = "ggraph"},
any layout string accepted by \code{ggraph::ggraph}.}

\item{edge_width_range}{Numeric vector of length 2. Minimum and maximum
line widths mapped to edge weights. Default \code{c(0.5, 4)}.}

\item{vertex_size}{Numeric. Vertex size for \code{engine = "igraph"}.
Default \code{30}.}

\item{vertex_color}{Character. Vertex fill colour.}

\item{edge_color}{Character. Edge colour.}

\item{label_size}{Numeric. Label character expansion. Default \code{0.8}.}

\item{title}{Character. Plot title. Default \code{NULL}.}

\item{...}{Additional arguments passed to the underlying renderer.}
}
\value{
For \code{engine = "igraph"}: invisibly returns \code{x} (called
  for its side effect).
  For \code{engine = "ggraph"}: a \code{ggplot} object.
}
\description{
Visualises the undirected weighted association network. The default
renderer uses \pkg{igraph}'s base-graphics plot. If \code{engine = "ggraph"}
is requested and \pkg{ggraph} is installed, a \pkg{ggplot2}-based plot is
returned as a \code{ggplot} object.
}
\details{
In v0.3.0 and earlier, the \code{layout} argument was silently ignored by
the igraph branch (Fruchterman-Reingold was always used). From 0.4.0
onwards, \code{layout} is respected for both engines.
}
\examples{
df <- expand_table(Titanic)
cg <- catgraph(df)
plot(cg)
plot(cg, layout = "kk")
plot(cg, layout = "circle")

}
