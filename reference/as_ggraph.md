# Coerce a catgraph to a ggraph-compatible tbl_graph

Coerce a catgraph to a ggraph-compatible tbl_graph

## Usage

``` r
as_ggraph(x, ...)
```

## Arguments

- x:

  A `catgraph` object.

- ...:

  Ignored.

## Value

A `tbl_graph` object (requires tidygraph).
